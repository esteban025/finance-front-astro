export enum HeadExpenses {
  Date = 'fecha',
  Amount = 'monto',
  Description = 'descripcion',
  Category = 'categoría',
  Subcategory = 'subcategoría',
}

export enum HeadSecondaries {
  Concept = 'concepto',
  Expected = 'previsto',
  Real = 'real',
  Difference = 'diferencia',
}

export enum Categories {
  Fixed = 'fijo',
  Variable = 'variable',
}