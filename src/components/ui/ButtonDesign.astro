---
interface Props {
  textAction?: string;
}

const { textAction = "Click me" }: Props = Astro.props;
---

<button
  class="bg-white-/5 rounded-lg border border-border p-1.5 cursor-pointer text-gray-400 transition-colors duration-200 hover:text-gray-200 relative group"
  title={textAction}
>
  <slot />
</button>
